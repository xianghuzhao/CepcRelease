category: external
version: 2.06.08

dep: [GCC, CMake]

path:
  lib: '{install}/lib'
  inc: '{install}/include'

source:
  url: http://lcgapp.cern.ch/project/simu/HepMC/download/HepMC-{version}.tar.gz
  file: HepMC-{version}.tar.gz
  main: HepMC-{version}

cmake:
  var:
    momentum: GEV
    length: MM

build: [cmake, make, make_install]

install:
  download: http
  pre_compile: env
