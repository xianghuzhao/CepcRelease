subdir: Simulation
version: 0.1.1

dep: [GCC, CMake, Geant4, MySQL, HepMC, Boost, ILCUTIL, LCIO, GEAR]

path:
  bin: '{install}/bin'
  lib: '{install}/lib'

source:
  url: http://cepcgit.ihep.ac.cn/cepcsoft/MokkaC.git
  tag: 'v{version}'

build: [cmake, make, make_install]

install:
  download: git
